import 'package:flutter/material.dart';

class HomeReviewScreen extends StatelessWidget {
  const HomeReviewScreen();

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(),
        const Text("復習画面"),
      ],
    );
  }
}
