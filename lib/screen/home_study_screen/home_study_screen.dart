import 'package:flutter/material.dart';

class HomeStudyScreen extends StatelessWidget {
  const HomeStudyScreen();

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(),
        Text("0"),
      ],
    );
  }
}
